#include<bits/stdc++.h>
using namespace std;

int binarysearch(int array[], int left, int right, int item){
    if(right>=left){

        // calculation of new mid
        int mid = left+(right - left)/2;

        // return position where found 

        if(array[mid] == item)
        return mid+1;

        // goes to recursive call in left half 
        if()

    }
}